# macOS and editor temporary files
.DS_Store
._*
*.swp
*.swo
*~

# Local Python environments, bytecode and tool caches
.venv/
venv/
__pycache__/
*.py[cod]
# Internal tool caches
/.cache/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/
.coverage
.coverage.*
htmlcov/

# Python build artifacts
/build/
/dist/
*.egg-info/

# Local credentials; keep shareable configuration templates
.env
.env.*
!.env.example
!.env.sample
!.env.template
.modal.toml

# Raw experiment runs stay local; share curated reports in results/.
/runs/

# Downloaded/trained model artifacts
/downloaded-adapter/
/downloaded-tokenizer/
*.safetensors
*.gguf
*.pt
*.pth
*.ckpt

# Keep datasets, uv lockfiles, reports and LAB_NOTEBOOK.md versioned.

# Visible local corpora, prepared tokens and dataset rebuilds
/datasets/local/

# Downloaded source spreadsheets; keep prepared questions and provenance
/datasets/prawko-*/source.xlsx
